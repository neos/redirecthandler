
Neos:
  RedirectHandler:
    features:
      hitCounter: true
    statusCode:
      redirect: 307
      gone: 410
    validation:
      path: '/^[a-z0-9\-\/\.]+$/i'
  Flow:
    http:
      chain:
        process:
          chain:
            redirect:
              position: 'after routing'
              component: Neos\RedirectHandler\RedirectComponent
